{% extends "../partials/layout.njk" %}
{% set mainClasses = "app-container govuk-body" %}

{% block content %}
  <h1>{{ applicationName }}</h1>
  <p>{{ __('home.line1') }}</p>
  <p>{{ __('home.line2') }}</p>
{% endblock %}
