{% macro getTitle(id, errors) %}
  {% set pageTitleId %}
    {%  %}
  {% endset %}
  {% if errors | findError(id) %}
    {% set pageTitle = __("error") + ": " + __(id + ".title") %}
  {% else %}
    {% set pageTitle = __(id + ".title") %}
  {% endif %}

  {{ pageTitle }} – {{ __("serviceName") }} – GOV.UK
{% endmacro %}
